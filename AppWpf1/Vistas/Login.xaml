<Window x:Class="AppWpf1.Vistas.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Inicio de Sesión"
        Height="250" Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Etiqueta y campo para la cédula -->
        <TextBlock Text="Cédula:" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox x:Name="txtCedula"
                 Grid.Row="0"
                 Margin="80,0,0,5"
                 Width="200"
                 HorizontalAlignment="Left"
                 MaxLength="10"
                 PreviewTextInput="TxtCedula_PreviewTextInput"
                 KeyDown="TxtCedula_KeyDown"/>

        <!-- Etiqueta y campo para la clave -->
        <TextBlock Text="Clave:" Grid.Row="1" Margin="0,10,0,5"/>
        <PasswordBox x:Name="txtClave"
                     Grid.Row="1"
                     Margin="80,10,0,5"
                     Width="200"
                     HorizontalAlignment="Left"
                     KeyDown="TxtClave_KeyDown"/>

        <!-- TextBox para mensaje de bienvenida -->
        <TextBox x:Name="txtBienvenida"
                 Grid.Row="2"
                 Margin="0,20,0,0"
                 Width="300"
                 Height="50"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Visibility="Collapsed"/>        
    </Grid>
</Window>